# 备案完成后：复制为 docker/Caddyfile，替换域名，执行
#   docker compose --profile https up -d
# 前置要求：.env 中必须设置 TRUST_PROXY=1，
# 否则限流以容器 IP 计数，所有用户共享同一个限流桶（全局 300/分、登录 10/分）
你的域名.com {
    reverse_proxy app:3000 {
        # SSE 流式响应必需：禁用代理缓冲
        flush_interval -1
    }
}
